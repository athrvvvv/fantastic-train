services:
  - type: worker
    name: bro-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: TELEG
        sync: false  # You will set this from the Render dashboard
